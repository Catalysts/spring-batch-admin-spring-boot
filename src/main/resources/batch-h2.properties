# Placeholders batch.*
#    for H2:
batch.jdbc.driver=org.h2.Driver
# use h2 in auto-server mixed-mode, very simple for local testing of spring-batch applications
batch.jdbc.url=jdbc:h2:C:/temp/mySpringBatchRun;AUTO_SERVER=TRUE
batch.jdbc.user=sa
batch.jdbc.password=
batch.drop.script=classpath:/empty.sql
batch.schema.script=classpath:/empty.sql
batch.business.schema.script=classpath:/empty.sql